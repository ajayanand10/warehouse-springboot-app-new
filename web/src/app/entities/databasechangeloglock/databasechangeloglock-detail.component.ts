//
// Project home: https://github.com/jaxio/celerio-angular-quickstart
// 
// Source code generated by Celerio, an Open Source code generator by Jaxio.
// Documentation: http://www.jaxio.com/documentation/celerio/
// Source code: https://github.com/jaxio/celerio/
// Follow us on twitter: @jaxiosoft
// This header can be customized in Celerio conf...
// Template pack-angular:web/src/app/entities/entity-detail.component.ts.e.vm
//
import {Component, OnInit, OnDestroy, Input, Output, EventEmitter} from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { SelectItem } from 'primeng/primeng';
import { MessageService} from '../../service/message.service';
import {Databasechangeloglock} from './databasechangeloglock';
import {DatabasechangeloglockService} from './databasechangeloglock.service';

@Component({
    moduleId: module.id,
	templateUrl: 'databasechangeloglock-detail.component.html',
	selector: 'databasechangeloglock-detail',
})
export class DatabasechangeloglockDetailComponent implements OnInit, OnDestroy {
    databasechangeloglock : Databasechangeloglock;

    private params_subscription: any;


    @Input() sub : boolean = false;
    @Output() onSaveClicked = new EventEmitter<Databasechangeloglock>();
    @Output() onCancelClicked = new EventEmitter();

    constructor(private route: ActivatedRoute, private router: Router, private messageService: MessageService, private databasechangeloglockService: DatabasechangeloglockService) {
    }

    ngOnInit() {
        if (this.sub) {
            return;
        }

        this.params_subscription = this.route.params.subscribe(params => {
            let id = params['id'];
            console.log('ngOnInit for databasechangeloglock-detail ' + id);

            if (id === 'new') {
                this.databasechangeloglock = new Databasechangeloglock();
            } else {
                this.databasechangeloglockService.getDatabasechangeloglock(id)
                    .subscribe(databasechangeloglock => {
                            this.databasechangeloglock = databasechangeloglock;
                        },
                        error =>  this.messageService.error('ngOnInit error', error)
                    );
            }
        });
    }

    ngOnDestroy() {
        if (!this.sub) {
            this.params_subscription.unsubscribe();
        }
    }

    onSave() {
        this.databasechangeloglockService.update(this.databasechangeloglock).
            subscribe(
                databasechangeloglock => {
                    this.databasechangeloglock = databasechangeloglock;
                    if (this.sub) {
                        this.onSaveClicked.emit(this.databasechangeloglock);
                        this.messageService.info('Saved OK and msg emitted', 'Angular Rocks!')
                    } else {
                        this.messageService.info('Saved OK', 'Angular Rocks!')
                    }
                },
                error =>  this.messageService.error('Could not save', error)
            );
    }

    onCancel() {
        if (this.sub) {
            this.onCancelClicked.emit("cancel");
            this.messageService.info('Cancel clicked and msg emitted', 'Angular Rocks!')
        }
    }

}
