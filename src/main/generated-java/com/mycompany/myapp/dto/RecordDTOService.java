/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Source code: https://github.com/jaxio/celerio/
 * Follow us on twitter: @jaxiosoft
 * This header can be customized in Celerio conf...
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.mycompany.myapp.dto;

import java.util.List;
import java.util.stream.Collectors;

import javax.inject.Inject;

import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Page;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.mycompany.myapp.domain.Record;
import com.mycompany.myapp.domain.Record_;
import com.mycompany.myapp.dto.support.PageRequestByExample;
import com.mycompany.myapp.dto.support.PageResponse;
import com.mycompany.myapp.repository.RecordRepository;

/**
 * A simple DTO Facility for Record.
 */
@Service
public class RecordDTOService {

    @Inject
    private RecordRepository recordRepository;

    @Transactional(readOnly = true)
    public RecordDTO findOne(Integer id) {
        return toDTO(recordRepository.findOne(id));
    }

    @Transactional(readOnly = true)
    public List<RecordDTO> complete(String query, int maxResults) {
        List<Record> results = recordRepository.complete(query, maxResults);
        return results.stream().map(this::toDTO).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public PageResponse<RecordDTO> findAll(PageRequestByExample<RecordDTO> req) {
        Example<Record> example = null;
        Record record = toEntity(req.example);

        if (record != null) {
            ExampleMatcher matcher = ExampleMatcher.matching() //
                    .withMatcher(Record_.data.getName(), match -> match.ignoreCase().startsWith());

            example = Example.of(record, matcher);
        }

        Page<Record> page;
        if (example != null) {
            page = recordRepository.findAll(example, req.toPageable());
        } else {
            page = recordRepository.findAll(req.toPageable());
        }

        List<RecordDTO> content = page.getContent().stream().map(this::toDTO).collect(Collectors.toList());
        return new PageResponse<>(page.getTotalPages(), page.getTotalElements(), content);
    }

    /**
     * Save the passed dto as a new entity or update the corresponding entity if any.
     */
    @Transactional
    public RecordDTO save(RecordDTO dto) {
        if (dto == null) {
            return null;
        }

        final Record record;

        if (dto.isIdSet()) {
            Record recordTmp = recordRepository.findOne(dto.id);
            if (recordTmp != null) {
                record = recordTmp;
            } else {
                record = new Record();
                record.setId(dto.id);
            }
        } else {
            record = new Record();
        }

        record.setData(dto.data);

        return toDTO(recordRepository.save(record));
    }

    /**
     * Converts the passed record to a DTO.
     */
    public RecordDTO toDTO(Record record) {
        return toDTO(record, 1);
    }

    /**
     * Converts the passed record to a DTO. The depth is used to control the
     * amount of association you want. It also prevents potential infinite serialization cycles.
     *
     * @param record
     * @param depth the depth of the serialization. A depth equals to 0, means no x-to-one association will be serialized.
     *              A depth equals to 1 means that xToOne associations will be serialized. 2 means, xToOne associations of
     *              xToOne associations will be serialized, etc.
     */
    public RecordDTO toDTO(Record record, int depth) {
        if (record == null) {
            return null;
        }

        RecordDTO dto = new RecordDTO();

        dto.id = record.getId();
        dto.data = record.getData();
        if (depth-- > 0) {
        }

        return dto;
    }

    /**
     * Converts the passed dto to a Record.
     * Convenient for query by example.
     */
    public Record toEntity(RecordDTO dto) {
        return toEntity(dto, 1);
    }

    /**
     * Converts the passed dto to a Record.
     * Convenient for query by example.
     */
    public Record toEntity(RecordDTO dto, int depth) {
        if (dto == null) {
            return null;
        }

        Record record = new Record();

        record.setId(dto.id);
        record.setData(dto.data);
        if (depth-- > 0) {
        }

        return record;
    }
}